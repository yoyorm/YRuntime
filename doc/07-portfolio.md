# 07 · 作品展示策略

> 生成于 2026-09-19。本文回答："**这个项目做完之后，怎么让它成为一份能被验证、能被讲清、能打动人的作品**。"
> 与 `00-vision.md` §4.5、`05-engineering.md` §9 配套。**每个里程碑收口时回来更新一次。**

---

## 1. 受众与定位

| 受众 | 他们看什么 | 你要准备什么 |
|---|---|---|
| 面试官（引擎/客户端/图形岗） | 你**是否真的理解**，以及能否讲清取舍 | 架构图 + 20 分钟脱稿讲解能力 + §5 的 30 道题 + 3 篇有观点的博客 |
| 开源社区 / 同好 | 代码质量、文档、可复现构建 | README 三条命令跑起来 + CI 徽章 + LICENSE + 设计文档公开 |
| 未来的你自己 | 当时为什么这么决定 | `06-decisions.md` + `notes/design/` + 周记（**这是最容易被忽略但价值最高的**） |
| 学校/竞赛/推免材料 | 完成度与规模的可量化证据 | 里程碑进度表、LOC、测试数、覆盖率、benchmark 数据、录屏 |

**定位一句话（可直接用于简历/自我介绍）**：
> 独立设计并实现了一个 C++20 游戏运行时（YRuntime）：从零实现句柄/反射/事件/场景树/序列化/异步资源/任务调度/主循环共 11 个模块，
> 用分层 CMake target + CI 强制依赖方向，Catch2 + ASan/TSan 全覆盖，并以此为基底跑通一个数据驱动的文字冒险 Demo，
> 最后将自研 Vulkan PBR 渲染器作为可替换 Backend 接入，实现 `游戏状态 → Runtime → Renderer → GPU` 的完整链路；
> 全程以 Godot 4.x 源码为参照系，产出 N 篇对照设计笔记。

（把 N、LOC、测试数等占位符在项目后期填成真实数字。**不要写没有证据支撑的形容词**，比如"高性能""工业级"。）

---

## 2. 证据链：如何证明"这是你自己做的、你真的懂"

这是本项目与 Yo_Renderer 最大的不同——Yo_Renderer 的 README 诚实标注了"使用 AI 辅助开发：仅为个人学习记录"，
而 YRuntime 要建立**可验证的独立完成证据**。四条证据，缺一不可：

| 证据 | 具体形式 | 从什么时候开始积累 |
|---|---|---|
| **① commit 历史节奏** | 小步、连续、message 规范、"设计→测试→实现→重构"可见；不是几次巨型提交 | M0 第一天 |
| **② 设计笔记先于代码** | `notes/design/*.md` 的 git 提交时间**早于**对应实现文件的提交时间（时间戳可验证） | M1 |
| **③ 可复述能力** | 每篇设计笔记的"我能解释吗"清单打勾 + 一段 5~20 分钟的讲解录音/视频（不必公开，自己留证） | 每个 M 收口 |
| **④ 有观点的取舍记录** | `06-decisions.md` 里每条 ADR 都有"备选与否决理由"+"什么情况下改回来"，且**至少 2 条被后续推翻并记录**（推翻记录比正确记录更有说服力） | 全程 |

> **诚实原则**：如果某个模块确实借助了 AI（比如 CMake 样板、CI yaml、Python 脚本），
> 在文件头或 commit message 里注明"AI 辅助生成，已逐行审查"。
> **`engine/` 下的核心实现代码必须自己写**（`05-engineering.md` §9.1 铁规则）。
> 一份标注清楚的作品比一份"看起来完美但说不清"的作品可信得多——面试官第一件事就是深挖细节。

---

## 3. 展示物清单（按里程碑产出，边做边攒，不要留到最后）

| 里程碑 | 展示物 | 形式 | 存放 |
|---|---|---|---|
| M0 | CI 徽章、构建三条命令 | README 片段 | 根 README |
| M1 | 容器 benchmark 报告（E1/E2/E4） | Markdown + 数据表 | `doc/notes/benchmarks/` |
| M2 | `yr_inspect` 反射查看器 | 终端截图 + GIF | `doc/media/` |
| M3 | 事件确定性重放测试 | 测试代码 + 日志对比 | `tests/event/` |
| M4 | headless 世界 tick 10000 帧 | **asciinema 录屏** + FrameStats CSV + p50/p95/max 表 | `doc/media/` |
| M5 | 手写 `.yrscn` + round-trip diff 为空 | 截图 + 格式规范文档 | `doc/notes/design/scene-format.md` |
| M6 | 慢 IO 下异步加载不卡帧（E10） | 帧时间曲线图（Python 画） | `doc/notes/benchmarks/` |
| M7 | 并行加速比曲线 + false sharing 实测（E11/E12） | 数据表 + 图 | `doc/notes/benchmarks/` |
| **M8** | **可玩文字冒险 Demo** | **录屏（mp4/GIF）+ 玩法 README + 自动通关 CI** | `apps/text_adventure/` + `doc/media/` |
| M9 | 快照 dump/replay + 渲染解耦设计笔记 | 博客素材 | `doc/notes/design/` |
| **M10** | **Runtime 驱动的 Vulkan 画面** | **GIF + 截图 + 架构图** | `doc/media/` |
| M11 | `.yrpak` 打包 + 覆盖率徽章 + 完整文档 | README + CI | 根目录 |

**视觉素材规范**
- 架构图：Mermaid（GitHub 原生渲染，可 diff，可维护）；**不要**用画图软件截图（改一次要重画一次）。
  最终版可另出一张精修 PNG 放 README 顶部（对照 Yo_Renderer 的 `doc/structure.png`）。
- 录屏：终端用 asciinema（体积小、可搜索、专业）；图形 Demo 用 mp4 → 裁成 ≤5s 的 GIF（< 5MB，GitHub README 能直接显示）。
- 数据图：Python + matplotlib，脚本存 `tools/plot_stats.py`（**脚本也是作品的一部分，证明数据可复现**）。
- 所有素材放 `doc/media/`，README 用相对路径引用。

---

## 4. 博客系列规划（写作倒逼理解，也是最好的公开证据）

**发布平台**：个人博客/GitHub Pages + 知乎或掘金（中文受众）。每篇 1500~3000 字，配代码片段与数据。

| # | 题目（暂定） | 对应 M | 核心论点（一句话） |
|---|---|---|---|
| 1 | 为什么我先做一个"看不见"的游戏引擎 | M0~M4 | headless-first 不是妥协，而是强迫解耦与缩短反馈回路的方法论 |
| 2 | 句柄与 generation：游戏引擎里最便宜的安全网 | M1 | index+generation 如何用 8 字节解决悬垂与 ABA，附 benchmark |
| 3 | 用 300 行宏换来一个反射系统（以及我为此付出的调试代价） | M2 | 宏工程的收益/代价权衡，附 `clang -E` 展开分析 |
| 4 | 一帧里的 14 件事：为什么顺序就是正确性 | M4 | 帧阶段设计 + 结构修改收敛到单一阶段如何消灭一整类 bug |
| 5 | 自己设计一种场景文件格式：引用、版本与循环三大难题 | M5 | 从 `.tscn` 学到的与我最终选择的格式 |
| 6 | 异步资源加载：为什么"最后一公里"必须回主线程 | M6~M7 | 线程边界画在数据上而不是函数上，附 Godot `ThreadLoadTask` 对照 |
| 7 | 我给游戏运行时加线程池踩的 6 个坑 | M7 | TSan/内存序/false sharing/停机死锁/浮点归约确定性 |
| 8 | 逻辑与渲染之间那条线：POD 快照契约 | M9~M10 | 为什么传对象指针的解耦都是假解耦，附"快照可重放"验证法 |
| 9 | 读 Godot 源码 8 个月，我改变了哪些设计判断 | 全程 | 三段式对照方法 + 具体被推翻的 ADR |
| 10 |（可选）从 8000 行 Vulkan 渲染器到可替换 Backend：一次搬迁复盘 | M10 | 搬迁时暴露的耦合点清单 |

**写作纪律**：① 每篇必须有一个**自己测出来的数据**或**自己踩出来的坑**，否则不写；
② 引用 Godot 源码时给文件路径（不给行号，版本会漂）；③ 结尾写"如果重做一次"。

---

## 5. 面试题库（30 题 + 好答案要点）

**用法**：每个里程碑收口时，把相关题目自问自答一遍（可口头、可写进 `notes/`）。
**好答案的通用结构**：我做了什么 → 为什么这么做 → 备选是什么、为什么不选 → 遇到的具体问题与数据 → 如果重做会怎么改。
只背概念不给取舍 = 不及格。

### 对象模型与内存（M1~M2）
1. 为什么用 `index+generation` 句柄而不是裸指针或 `shared_ptr`？generation 位数怎么定？回绕了怎么办？
   *要点*：悬垂可检测、紧凑存储、cache 友好；`shared_ptr` 控制块分离且掩盖生命周期错误；回绕用 E1 数据 + 概率分析回答。
2. `SlotMap` 的删除为什么是 O(1)？稠密数组 + 空闲链的具体结构？删除后指针/迭代器失效怎么处理？
3. 侵入式引用计数与 `std::shared_ptr` 的差别？各自的代价？（*要点*：控制块、`sizeof`、侵入基类、跨模块 ABI）
4. 引用计数如何处理循环引用？你的 `WeakRef` 怎么实现？为什么 parent 指针不用 Ref？
5. 你的反射系统如何做到"新增一个类不改任何中心文件"？静态初始化顺序问题（SIOF）怎么解决？
6. 属性 get/set 的类型擦除具体怎么做的？为什么不用虚函数？三种擦除方案（成员指针+void* / `std::function` / 模板）的取舍？
7. 为什么自己写 Variant 而不用 `std::variant`？`sizeof(Variant)` 是多少，怎么算出来的？
8. 字符串驻留（StringId）解决了什么？哈希冲突怎么办？驻留表的内存与线程安全？

### 场景与生命周期（M3~M4）
9. 一个节点在自己的 `process` 里删除自己/父节点/兄弟节点，分别会发生什么？你怎么保证不崩？
   *要点*：`queue_free` + 阶段 9 统一 flush + 父子同删去重 + 遍历用双缓冲列表。这题最能区分"写过"和"读过"。
10. `_ready` 为什么是后序、`_process` 为什么是前序？如果反过来会出什么问题？
11. 事件总线的"立即派发"和"队列派发"你为什么两个都要？重入（回调里退订自己、回调里再发同一事件）怎么处理？
12. 如何保证"同样的输入两次运行产生逐字节相同的日志"？哪些地方会破坏确定性？
    *要点*：容器遍历顺序（保序 Dict）、浮点归约顺序、指针地址不入日志、并行任务结果顺序、时间源可注入。
13. 固定步长累加器怎么写？spiral of death 是什么、你怎么防？渲染插值为什么需要（可以只讲原理）？
14. 一帧里的 14 个阶段，为什么 `pump_assets` 在 `process` 之前而不是之后？为什么 GC 在最后？
15. Transform 层级的脏标记怎么传播？向上还是向下？多线程下会遇到什么问题？（附 E8 数据）

### 序列化与资源（M5~M6）
16. 你的场景格式如何处理"两个节点引用同一个资源"？外部资源与内嵌子资源的区别？
17. 循环引用（A→B→A）怎么序列化？为什么必须两遍法？
18. 老版本存档在新版本打不开，你的兼容策略是什么？未知属性和缺失属性分别怎么处理？
19. 为什么文本格式要求"输出逐字节可复现"？哪些实现细节会破坏它？（*要点*：保序容器、浮点格式化精度、时间戳/指针不入文件）
20. 二进制格式里结构体填充（padding）和字节序怎么处理？为什么不能直接 `fwrite(struct)`？
21. 资源为什么要 UID 而不只用路径？文件重命名/移动时发生了什么？
22. 异步加载的完整状态机？为什么解码可以在工作线程而对象构造必须回主线程？（D8）
23. 同一个资源被并发请求 5 次会发生什么？加载中被请求卸载呢？加载失败后 in-flight 条目怎么清理？
24. 资源什么时候卸载？refcount==1 的 GC 为什么会有"临时 Ref 导致本帧不卸载"的现象，这是 bug 吗？

### 并发（M7）
25. 你的任务依赖是怎么调度的？主线程在 `wait` 时做什么？为什么不能 sleep？
26. `memory_order_acquire/release` 在你的队列里具体保护什么？哪些地方可以用 relaxed，为什么？
27. false sharing 是什么？你在哪里遇到过、怎么发现的、怎么解决的？（附 E12 数据）
28. 并行化之后如何保证结果与串行一致？浮点求和为什么是个问题？
29. `~JobSystem()` 的停机流程？如果还有任务在跑、有任务在等待别的任务，怎么避免死锁？
30. TSan 报了一个你认为是误报的竞争，你怎么处理？（*要点*：先假设不是误报；suppress 文件要写理由）

### 渲染解耦与工程（M9~M11）
31. Runtime 与 Renderer 的边界你画在哪里？为什么是快照而不是接口回调？
32. 如果换成 OpenGL 后端，Runtime 需要改几行代码？你怎么**证明**这一点？（*要点*：CI grep 门禁 + NullBackend 跑全部测试 + 快照可重放）
33. Backend 里的 GPU 资源生命周期怎么管？Runtime 卸载资源时 GPU 侧正在用怎么办？
    *要点*：generation + frame-in-flight 延迟销毁（Yo_Renderer 的 `VulkanResourceTable` 已实践过，能讲清就是加分项）
34. 快照双缓冲/三缓冲的同步怎么做？Backend 落后 Runtime 两帧会怎样？
35. 你的 CMake 分层如何**物理上**阻止反向依赖？除了 CMake 还有什么机制？
36. 为什么禁用 `file(GLOB)`？为什么 `-Werror` 只在 CI 开？
37. CI 上无 GPU 怎么测渲染相关代码？
38. 项目里最难的一个 bug 是什么，你怎么定位的？（**必须有一个真实故事**，来自 `notes/` 的排查记录）
39. 哪个设计你做错了、后来推翻了？为什么当时会那么想？（**必须有真实答案**，来自被推翻的 ADR）
40. 如果给你三个月继续做，你会先做什么？为什么？（考察优先级判断，答案要从 Stretch 清单里选并给理由）

---

## 6. 简历条目模板

**项目条目（3~5 行，按投递岗位裁剪）**
> **YRuntime · 个人项目**（C++20 / CMake / Vulkan / Linux，XXXXX 行，XX 个测试，CI 全绿）
> 独立设计实现游戏运行时核心：句柄+generation 的对象生命周期管理、基于宏与类型擦除的反射/属性系统、
> 类型化事件总线与延迟调用队列、场景树与 14 阶段固定步长主循环、自研文本/二进制场景序列化格式（支持外部/内嵌资源引用与版本迁移）、
> 异步资源数据库（IO/解码并行 + 主线程收尾）、任务依赖调度系统。
> 以分层 CMake target + CI 脚本强制依赖单向；Catch2 + ASan/UBSan/TSan 全覆盖；核心模块行覆盖率 XX%。
> 将自研 Vulkan PBR 渲染器重构为可替换 RendererBackend（POD 快照契约，Runtime 侧零图形 API 依赖），跑通完整"逻辑—运行时—渲染"链路。

**技能条目（从项目里长出来的，不要空写）**
> C++20（模板/变参/concepts/移动语义/原子与内存模型）、CMake+Ninja 分层构建、Catch2 单测与 Sanitizer、
> 多线程任务调度、序列化格式设计、Vulkan（dynamic rendering / synchronization2 / frame-in-flight / 延迟销毁）、
> Linux 调试（gdb / perf / ASan-TSan）、Godot 4.x 源码研读

**避免的写法**：❌"熟悉 C++"、❌"了解游戏引擎架构"、❌"高性能"、❌"工业级"、❌ 没有数字的形容词。
**推荐的写法**：✅ 每个技能点后面能立刻接一个"我在 X 模块用它解决了 Y 问题，数据是 Z"。

---

## 7. 开源与合规

- [ ] LICENSE：MIT（与 Yo_Renderer 一致）。放根目录，源文件头不加版权注释（太吵），README 注明。
- [ ] 第三方归属：`third_party/` 各库的 LICENSE 保留；README 有"依赖与致谢"小节列出 nlohmann_json / GLM / Catch2 / GLFW / Vulkan SDK / ImGui（若用）。
- [ ] 引用 Godot：README 与博客里明确"本项目以 Godot Engine 源码为学习参照系，非官方关联，Godot 采用 MIT 许可"。
  **不要复制 Godot 代码**（哪怕它是 MIT，也要注明出处；最好只借鉴思路，自己写）。
- [ ] 资源素材：文字游戏的文本自己写或用 CC0 素材；M10 的模型/贴图沿用 Yo_Renderer 的（已确认授权）并注明来源。
- [ ] 仓库整洁：无 build 产物、无大二进制、无 `.vscode` 个人配置（或只留 `settings.json`/`launch.json` 模板）、无空目录。
- [ ] README 有"如何在 15 分钟内跑起来"，并**找一个真实的人实测一次**（`02-roadmap.md` M11 验收项）。

---

## 8. 展示节奏（把"攒作品"变成流程，而不是最后一周补救）

| 时机 | 动作 |
|---|---|
| 每个 M 收口 | 存录屏/截图/benchmark → 更新根 README 的里程碑进度表 → 更新本文 §3 打勾 |
| 每 2~3 个 M | 写一篇博客（§4）→ 公开仓库（**从 M0 就公开**，不要等"完美了再公开"，历史 commit 是资产） |
| M8 完成 | **第一个可展示节点**：更新简历项目条目初稿 + 录一段 3 分钟讲解视频（自己看，找讲不清的地方） |
| M10 完成 | **第二个可展示节点**：架构图定稿 + README 重写 + 博客 8/9/10 |
| M11 完成 | 全量打磨：覆盖率、文档、README 实测、简历终稿、面试题库自测（§5 全部能答） |

**最重要的一条**：**仓库从第一天就 public。**
一个有 300 个规范 commit、跨越 8 个月的仓库，比一个"某天突然出现、commit 只有 20 个"的仓库可信度高一个量级。
